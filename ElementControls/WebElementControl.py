# here we will define the webdriver, 
# we will also define how to get ChromeDriver, Firefox Driver(GeckoDriver), Safari Driver etc.
# Tester will call this class and pass the driver name, which one they wants